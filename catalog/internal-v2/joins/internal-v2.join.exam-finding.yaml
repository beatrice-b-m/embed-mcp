kind: join
join_type: hierarchy
relationships: [clinical.exam-finding]
source_columns: [internal-v2.magview_all_cohorts_pacs_v2_anon#acc_anon]
source_completeness: required
target_columns: [internal-v2.magview_all_cohorts_pacs_v2_anon#acc_anon]
cardinality:
  targets_per_source: zero_or_more
  sources_per_target: zero_or_more
evidence: [release_schema, observed_source_values]
hazards:
  - >-
    Repeated wide rows multiply co-located object occurrences;
    do not treat row multiplicity as clinical multiplicity without declared grouping.
# Prototype: cites internal-v2.magview-representation-context#finding-identity-scope,
# which is outside this slice.
caveats:
  - This same-table binding records represented clinical association, not a separate-table join or one-to-one attribution.
