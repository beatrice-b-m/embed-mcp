kind: join
join_type: hierarchy
source_completeness: required
cardinality:
  targets_per_source: zero_or_one
  sources_per_target: zero_or_more
evidence: [release_schema, inference, unresolved]
hazards:
  - Use accession for the exam edge; retain side only for side-grain identity.
caveats:
  - The exam hierarchy is expected but not established as an enforced referential constraint.
relationships: [clinical.exam-side]
source_columns: [open-v2.side_level_anon#acc_anon]
target_columns: [open-v2.exam_level_anon#acc_anon]
