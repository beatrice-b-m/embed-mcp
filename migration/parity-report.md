# Legacy conversion parity report

> Generated by `migration/convert_legacy.py`; do not edit. It accounts for
> every legacy value under `legacy/catalog/` against
> `temp-docs/parity-ledger.md`.

## Accounting

Every legacy value was accounted for. Counts are per legacy value or subtree:

| Legacy file | kept | renamed | link | typed | derived | module | dropped |
|---|---|---|---|---|---|---|---|
| semantic | 987 | 348 | 784 | 50 | 116 | 20 | 24 |
| internal-v2 | 2614 | 2168 | 1396 | 191 | 390 | 295 | 25 |
| open-v2 | 955 | 1608 | 796 | 232 | 325 | 44 | 66 |

## Dropped values

| Reason | Count |
|---|---|
| catalog-development note | 29 |
| empty | 10 |
| null | 2 |
| portable caveat contradicted by internal-v2 (maintainer decision D3.1) | 16 |
| restates evidence_status | 52 |
| the model is the schema | 6 |

## Conditional conversions that did not apply

None: every condition held.

## Result

`embed-context check` on the converted catalog: 0 errors, 0 warnings.


649 documents, 926 entries, 3754 links.

| Kind | Documents |
|---|---|
| aggregation | 5 |
| clinical_object | 17 |
| context | 19 |
| feature | 218 |
| guardrail | 21 |
| join | 42 |
| join_path | 1 |
| profile_support | 90 |
| relationship | 28 |
| source | 34 |
| table | 13 |
| temporal | 7 |
| topic | 16 |
| vocabulary | 138 |

| Link type | Links |
|---|---|
| about | 170 |
| applies_to | 188 |
| cites | 404 |
| claim_sources | 274 |
| evidence_cites | 111 |
| exception_column | 5 |
| feature_temporal | 17 |
| identity_columns | 17 |
| input_feature | 5 |
| interpretation_feature | 8 |
| join_relationships | 35 |
| key_columns | 66 |
| maps | 469 |
| maps.vocabulary | 231 |
| object | 42 |
| object_columns | 212 |
| objects | 233 |
| path_joins | 2 |
| path_relationship | 1 |
| relationship_temporal | 45 |
| relative_to | 8 |
| representation_cites | 48 |
| result_feature | 3 |
| source_columns | 99 |
| source_object | 33 |
| step_claims | 5 |
| subject | 90 |
| target_columns | 99 |
| target_object | 33 |
| temporal_objects | 11 |
| topics | 780 |
| via | 10 |

## Prose that may describe the catalog rather than EMBED

These sentences mention the catalog or its development. They were kept;
review whether each describes EMBED (D3.2).

| Document | Field | Text |
|---|---|---|
| `internal-v2.cancer-history-context` | claims.relative-grouping-not-identity.statement | The packet tests (acc_anon, patient, rel) under the label RELATIVE, finer than EXAM. The remaining history attributes, including curr_age, brca1, and brca2, vary within that grouping; the tuple is not a unique row key and does not establish one identifiable relative. |
| `internal-v2.cancer-history-context` | claims.conditional-code-structure.caveats | The packet does not establish that every entry is malignant, nor that an absent history entry is cancer absence. |
| `internal-v2.history-topology-context` | definition | Reviewed topology packets and the complete-inventory clarification support history table inventories and existing HormoneHist and ProcHist mappings, while preserving unresolved row identity, conditional code discrepancies, and time semantics. |
| `internal-v2.history-topology-context` | claims.physical-inventories.statement | The packet columns plus the maintainer-confirmed comment field form the complete column inventories of HormoneHist_anon, ProcedureHist_anon, and CancerHist_anon. Packet str is cataloged as string and int64 as int64; comment is assessed as string from its free-text description. Every column is conservatively nullable because pandas dtypes and observed levels do not declare source-schema nullability. |
| `internal-v2.history-topology-context` | claims.tested-groupings.statement | In each reviewed packet, the tested PATIENT (empi_anon) grouping is coarser than EXAM (acc_anon), and empi_anon is constant within acc_anon. HormoneHist and ProcHist history attributes vary within the tested patient and exam groupings, except the blank HormoneHist side field. Neither grouping identifies a history row. |
| `internal-v2.history-topology-context` | claims.hormone-category-discrepancies.statement | After comparing the padded source tokens with the existing category-specific dictionaries, the packet represents ORAL under H and T and TAXOL under H. Their meanings in those category occurrences remain unresolved; the supported ORAL contraceptive and TAXOL therapy meanings do not establish these additional occurrences. |
| `internal-v2.history-topology-context` | claims.hormone-category-discrepancies.caveats | A documented code absent from this packet is not invalidated, and vocabulary completeness remains unknown. |
| `internal-v2.history-topology-context` | claims.hormone-status-representation.statement | HormoneHist current and continuous have Y, N, and blank string representations in the packet. Their reported-status concepts remain applicable, but the packet alone does not establish a blank-as-N rule or exact interval endpoints. |
| `internal-v2.magview-coded-values-context` | claims.unlisted-source-codes.caveats | Unexplained tokens are retained as unresolved rather than guessed or silently normalized. |
| `internal-v2.magview-representation-context` | claims.patient-attribute-variation.caveats | The catalog does not select a universal latest-row policy. |
| `internal-v2.patient-history-context` | caveats | The controlled code meanings remain supported, while packet-observed category discrepancies and composite result semantics are separately unresolved. |
| `internal-v2.patient-history-context` | claims.history-identity-and-physical-scope.statement | Complete physical inventories and supported mappings are now cataloged from reviewed packets plus maintainer clarification, but no stable history-entry identifier or semantic deduplication rule is established. |
| `internal-v2.v1c-metadata-context` | claims.current-v1c-coverage.caveats | Secondary captures and screen saves are excluded except ROI_SS and ROI_SSC images retained solely to enable extraction of radiologist annotations. |
| `internal-v2.v1c-metadata-context` | claims.modality-surface.statement | Internal V1c extracted DICOM objects associated with every EMBEDv1 accession, then excluded secondary captures and screen saves except ROI_SS and ROI_SSC annotation images retained solely for ROI extraction; represented source modalities include mammography, ultrasound, and other. |
| `internal-v2.v1c-metadata-context` | claims.roi-screen-capture-purpose.statement | ROI_SS and ROI_SSC are retained secondary screen-capture images containing burned-in radiologist pixel annotations; an annotation-extraction model converted those annotations to coordinates that were transferred to the corresponding original images, and the screen captures are not intended for any other use. |
| `internal-v2.v1c-metadata-context` | claims.patient-and-exam-projection.caveats | Each accession must associate with exactly one patient; a cross-patient accession association is an invalid data-quality error that must not be retained as a valid identity or linkage association. |
| `internal-v2.v1c-metadata-context` | claims.burned-in-annotation-semantics.caveats | The catalogue does not prescribe exclusion, redaction, or another action based on this attribute. |
| `internal-v2.image.burned_in_annotation_flag` | caveats | The catalogue does not prescribe an action for YES, NO, or absence. |
| `internal-v2.image.derived_image_type` | caveats | ROI_SS and ROI_SSC are secondary screen-capture annotation-transfer artifacts retained solely for ROI extraction and are not intended for other use. |
| `internal-v2.image.dicom_conversion_type` | caveats | Secondary captures and screen saves were excluded except ROI_SS and ROI_SSC annotation images retained solely for ROI extraction. |
| `internal-v2.image.entrance_dose_mgy` | caveats | Other represented dose and exposure attributes retain their standard DICOM element meanings but remain inventoried without dedicated catalogue concepts. |
| `internal-v2.pathology.tnm_stage_comment` | caveats | This is not a normalized stage category and its content is not exposed by the catalog. |
| `internal-v2.join.exam-hormone-history` | caveats | Descriptive route through established identifier namespaces; cross-table match coverage was not tested by these packets. |
| `internal-v2.join.exam-image` | hazards | Use the anonymized patient identifier for cross-table patient identity and as a consistency check rather than as an exam-identity component; a cross-patient accession association is invalid and must not be retained as a valid linkage. |
| `internal-v2.join.exam-procedure-history` | caveats | Descriptive route through established identifier namespaces; cross-table match coverage was not tested by these packets. |
| `internal-v2.join.patient-hormone-history` | caveats | Descriptive route through established identifier namespaces; cross-table match coverage was not tested by these packets. |
| `internal-v2.join.patient-procedure-history` | caveats | Descriptive route through established identifier namespaces; cross-table match coverage was not tested by these packets. |
| `internal-v2.history-inventory-maintainer-confirmation` | version_scope | Column completeness of the reviewed HormoneHist, ProcedureHist, and CancerHist packets. |
| `internal-v2.v1c-metadata-maintainer-clarifications` | notes | acc_anon is the exam identifier across EMBED; a cross-patient association is a data-quality defect that must not be retained as a valid identity or linkage association and does not redefine the accession as several exams. |
| `internal-v2.v1c-metadata-maintainer-clarifications` | notes | All DICOM objects associated with EMBEDv1 accessions were extracted, after which secondary captures and screen saves were excluded except ROI_SS and ROI_SSC annotation images retained solely to enable ROI extraction. |
| `internal-v2.support.patient-history-physical-binding` | representation_summary | HormoneHist_anon and ProcedureHist_anon have complete physical inventories from reviewed packet columns plus the confirmed comment field, and bindings for supported history semantics. |
| `internal-v2.support.patient-history-physical-binding` | representation_caveats | Packet dtypes are parse observations, comment is assessed as text, and all columns are conservatively nullable. |
| `internal-v2.support.v1c-dicom-attribute-interpretation` | representation_summary | Inventoried internal V1c columns parsed directly from DICOM retain standard element semantics and received no processing beyond anonymization; many acquisition, dose, detector, collimator, geometry, windowing, icon-image, order-number, and time-of-day attributes remain physically inventoried without dedicated catalogue concepts. |
| `internal-v2.support.v1c-dicom-attribute-interpretation` | representation_caveats | An unmapped parsed DICOM column is not semantically unknowable: it can be mapped using the standard element meaning when a dedicated catalogue concept is needed. |
| `internal-v2.support.v1c-image-modality-coverage` | representation_summary | Internal V1c covers DICOM objects associated with the complete EMBEDv1 accession set after excluding secondary captures and screen saves, except ROI_SS and ROI_SSC images retained solely for ROI extraction. |
| `internal-v2.cancerhist_anon` | caveats | Physical types are packet-reported pandas parse types, with str represented as string; comment is assessed as string from maintainer description rather than packet observation. |
| `internal-v2.cancerhist_anon` | caveats | The packet tests groupings only and does not establish unique row identity, deduplication, or cross-table match coverage. |
| `internal-v2.cancerhist_anon` | keys.patient-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.cancerhist_anon` | keys.exam-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.cancerhist_anon` | keys.relative-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.hormonehist_anon` | caveats | Physical types are packet-reported pandas parse types, with str represented as string; comment is assessed as string from maintainer description rather than packet observation. |
| `internal-v2.hormonehist_anon` | caveats | The packet tests groupings only and does not establish unique row identity, deduplication, or cross-table match coverage. |
| `internal-v2.hormonehist_anon` | keys.patient-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.hormonehist_anon` | keys.exam-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.hormonehist_anon` | columns.duration.maps.notes | Packet int64 is a physical parse type, not evidence for duration units or zero-as-missing semantics. |
| `internal-v2.metadata_all_cohorts_v1c` | caveats | DICOM objects associated with EMBEDv1 accessions were extracted, then secondary captures and screen saves were excluded except ROI_SS and ROI_SSC images retained solely for ROI extraction. |
| `internal-v2.metadata_all_cohorts_v1c` | caveats | Detailed DICOM acquisition, dose, detector, collimator, geometry, windowing, and icon-image attributes retain standard DICOM element semantics but remain inventoried without dedicated catalogue concepts or mappings. |
| `internal-v2.metadata_all_cohorts_v1c` | objects.imaging_exam.caveats | Association with more than one patient identifier is an invalid data-quality error and must not be retained as a valid identity or linkage association. |
| `internal-v2.metadata_all_cohorts_v1c` | columns.PatientSex.interpretations.DICOM PatientSex value.meaning | The source DICOM patient-sex value, retained without modification during anonymization |
| `internal-v2.procedurehist_anon` | caveats | Physical types are packet-reported pandas parse types, with str represented as string; comment is assessed as string from maintainer description rather than packet observation. |
| `internal-v2.procedurehist_anon` | caveats | The packet tests groupings only and does not establish unique row identity, deduplication, or cross-table match coverage. |
| `internal-v2.procedurehist_anon` | keys.patient-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.procedurehist_anon` | keys.exam-grouping.caveats | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. |
| `internal-v2.codes.hormone-history-hormone` | caveats | The reviewed topology packet also represents ORAL under H and T and TAXOL under H; these category occurrences have unresolved meanings and are not assigned meanings from another category. |
| `internal-v2.codes.hormone-history-therapy` | caveats | The reviewed topology packet also represents ORAL under H and T and TAXOL under H; these category occurrences have unresolved meanings and are not assigned meanings from another category. |
| `internal-v2.codes.image-derived-image-type` | codes.ROI_SS | secondary screen-capture image containing burned-in radiologist pixel annotations, retained solely to enable ROI coordinate extraction and transfer to the original image |
| `internal-v2.codes.image-derived-image-type` | codes.ROI_SSC | secondary screen-capture image containing burned-in radiologist pixel annotations, retained solely to enable ROI coordinate extraction and transfer to the original image |
| `internal-v2.codes.image-derived-image-type` | caveats | ROI_SS and ROI_SSC are secondary screen-capture annotation-transfer artifacts retained solely for ROI extraction and are not intended for other use. |
| `open-v2.multimodal-finding-context` | claims.finding-side-null.caveats | Interpret null laterality according to the field's clinical role rather than applying a catalog-wide null rule. |
| `open-v2.pathology-procedure-context` | claims.patient-aggregate-absence.caveats | Absence of a supplied field does not prohibit an analyst-defined policy; it means the catalog does not designate one. |
| `open-v2.temporal-availability-context` | caveats | Task-specific cohort, follow-up, label, and modality-inclusion choices are analysis policies rather than dataset claims; agents must choose and defend them outside the portable catalog. |
| `open-v2.join.imaging-findings-anon-exam` | caveats | The accession relationship is expected, but complete referential coverage was not retained as a portable claim. |
| `open-v2.join.side-level-anon-exam` | caveats | The expected exam hierarchy was not retained as an enforced referential claim. |
| `open-v2.catalog-linkage-review` | version_scope | Retained open-v2 table, key, projection, relationship, and caveat conclusions derived during the schema-version-3 review. |
| `open-v2.maintainer-clarification-2026-07-29` | version_scope | Open-v2 linked-exam, multimodal descriptor, aggregation, pathology, report, risk, demographic, laterality, and date semantics reviewed on 2026-07-29 and retained in the repository clarification record. |
| `open-v2.combined_anon` | keys.export_index.caveats | The retained index is release-local construction provenance, not a clinical key or current row position. |
| `open-v2.exam_level_anon` | keys.patient_accession.caveats | The composite is redundant because the accession identifier is itself unique; it is retained for a directly checked wide-table relationship. |
| `open-v2.side_level_anon` | keys.accession_side.caveats | Release uniqueness retained null side as a state; this is not a SQL uniqueness constraint over nullable columns. |
| `open-v2.codes.pathology-severity` | caveats | Internal raw value 6 is not retained in the canonical exported context. |
| `aggregation.pathology-severity-to-finding` | caveats | The catalog does not select a finding-level outcome policy. |
| `aggregation.pathology-severity-to-patient` | caveats | The catalog does not define incident, prevalent, earliest, or ever-positive patient outcomes. |
| `guardrail.attached-pathology-selection` | caveats | The catalog identifies the selection mechanism but does not prescribe an inclusion, exclusion, weighting, or causal-analysis policy. |
| `guardrail.explicit-grain-aggregation` | caveats | The catalog does not select a canonical cohort or patient-level outcome. |
| `guardrail.longitudinal-boundaries-are-analyst-choices` | rationale | These choices determine temporal membership and multiplicity but are not representation failures, and the catalog does not select them. |
| `guardrail.non-breast-cancer-not-no-malignancy` | caveats | Whether this state is eligible for any task-specific comparison group is an analysis decision outside the catalog. |
| `guardrail.null-pathology-not-negative` | caveats | A task-specific study may measure follow-up, but the catalog does not supply that policy. |
| `module` | notices | The catalog describes representation and does not prescribe care. |
| `breast_side` | caveats | The semantic object must not inherit one catalog-wide null-laterality rule. |
| `image` | caveats | Catalog coverage of an image object does not imply that a selected profile has an image table or file binding. |
| `patient` | caveats | The catalog does not establish complete longitudinal care or outside-system follow-up. |
| `clinical.exam-image` | attribution_limitations | The catalog does not define a portable physical identifier or file-layout rule for associating images with exams. |

