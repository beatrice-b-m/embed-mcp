[project]
name = "embedv2-agent-context"
version = "0.10.0"
description = "Count-free clinical-semantic context for EMBED data"
readme = "README.md"
authors = [
    { name = "Beatrice Brown-Mulry" },
]
requires-python = ">=3.11,<3.14"
dependencies = [
    "jsonschema==4.26.0",
]
keywords = ["EMBED", "breast imaging", "clinical semantics", "MCP"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3 :: Only",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Scientific/Engineering :: Medical Science Apps.",
]

[project.urls]
Repository = "https://github.com/beatrice-b-m/embed-mcp"
Issues = "https://github.com/beatrice-b-m/embed-mcp/issues"
Documentation = "https://github.com/beatrice-b-m/embed-mcp#readme"
"EMBED Documentation" = "https://docs.hitilab.com/datasets/embed"

[project.scripts]
embed-context = "embed_context.cli:main"
embed-context-mcp = "embed_context.mcp_server:main"

[project.optional-dependencies]
mcp = [
    "mcp==2.0.0",
]
curator = [
    "embedv2-agent-context-curator==0.10.0",
]

[dependency-groups]
dev = [
    "pyarrow==20.0.0",
]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["embed_context"]

[tool.hatch.build.targets.wheel.force-include]
"catalog/catalog-set.json" = "embed_context/_data/catalog-set.json"
"catalog/internal-v2-catalog-set.json" = "embed_context/_data/internal-v2-catalog-set.json"
"catalog/catalog-set.schema.json" = "embed_context/_data/catalog-set.schema.json"
"catalog/semantic/catalog.json" = "embed_context/_data/semantic/catalog.json"
"catalog/semantic/catalog.schema.json" = "embed_context/_data/semantic/catalog.schema.json"
"catalog/profiles/open-v2.json" = "embed_context/_data/profiles/open-v2.json"
"catalog/profiles/internal-v2.json" = "embed_context/_data/profiles/internal-v2.json"
"catalog/profiles/profile.schema.json" = "embed_context/_data/profiles/profile.schema.json"
"catalog/extensions/extension.schema.json" = "embed_context/_data/extensions/extension.schema.json"
[tool.uv]
package = true

[tool.uv.sources]
embedv2-agent-context-curator = { workspace = true }
embedv2-agent-context = { workspace = true }

[tool.uv.workspace]
members = ["packages/curator"]
