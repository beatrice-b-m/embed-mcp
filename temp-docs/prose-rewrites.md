# Prose rewrites after migration

> Working record of the post-migration content edit (rebuild contract,
> section 6.4, item 1). Each sentence that described the catalog rather than
> EMBED was rewritten in place in `catalog/`, wherever it occurred.

## Group 1: EMBED facts stated directly

| Where | Before | After |
|---|---|---|
| `image` | Catalog coverage of an image object does not imply that a selected profile has an image table or file binding. | The image object does not imply that every profile has an image table or file binding. |
| `internal-v2.cancer-history-context` | The packet does not establish that every entry is malignant, nor that an absent history entry is cancer absence. | Not every entry is established as malignant, and an absent history entry does not establish cancer absence. |
| `internal-v2.cancer-history-context` | The packet tests (acc_anon, patient, rel) under the label RELATIVE, finer than EXAM. The remaining history attributes, including curr_age, brca1, and brca2, vary within that grouping; the tuple is not a unique row key and does not establish one identifiable relative. | The (acc_anon, patient, rel) grouping, labeled RELATIVE, is finer than EXAM. The remaining history attributes, including curr_age, brca1, and brca2, vary within that grouping; the tuple is not a unique row key and does not identify one relative. |
| `internal-v2.cancerhist_anon`, `internal-v2.hormonehist_anon`, `internal-v2.procedurehist_anon` | Physical types are packet-reported pandas parse types, with str represented as string; comment is assessed as string from maintainer description rather than packet observation. | Physical types are parse types rather than source-declared types; comment is a free-text string. |
| `internal-v2.cancerhist_anon`, `internal-v2.hormonehist_anon`, `internal-v2.procedurehist_anon` | The packet tests groupings only and does not establish unique row identity, deduplication, or cross-table match coverage. | No unique row identity, deduplication rule, or cross-table match coverage is established. |
| `internal-v2.cancerhist_anon`, `internal-v2.hormonehist_anon`, `internal-v2.procedurehist_anon` | A tested grouping with varying attributes, not a unique history-row or clinical-entry identity; missing values were retained as levels. | A grouping within which attributes vary; it is not a unique history-row or clinical-entry identity. |
| `internal-v2.codes.hormone-history-hormone`, `internal-v2.codes.hormone-history-therapy` | The reviewed topology packet also represents ORAL under H and T and TAXOL under H; these category occurrences have unresolved meanings and are not assigned meanings from another category. | ORAL also occurs under H and T, and TAXOL under H; these category occurrences have unresolved meanings and are not assigned meanings from another category. |
| `internal-v2.history-topology-context` | A documented code absent from this packet is not invalidated, and vocabulary completeness remains unknown. | A documented code is not invalidated by being unobserved, and vocabulary completeness remains unknown. |
| `internal-v2.history-topology-context` | After comparing the padded source tokens with the existing category-specific dictionaries, the packet represents ORAL under H and T and TAXOL under H. Their meanings in those category occurrences remain unresolved; the supported ORAL contraceptive and TAXOL therapy meanings do not establish these additional occurrences. | Outside the category-specific dictionaries, the padded source tokens include ORAL under H and T and TAXOL under H. Their meanings in those category occurrences remain unresolved; the supported ORAL contraceptive and TAXOL therapy meanings do not establish these additional occurrences. |
| `internal-v2.history-topology-context` | HormoneHist current and continuous have Y, N, and blank string representations in the packet. Their reported-status concepts remain applicable, but the packet alone does not establish a blank-as-N rule or exact interval endpoints. | HormoneHist current and continuous are represented as Y, N, and blank strings. Their reported-status concepts remain applicable, but no blank-as-N rule or exact interval endpoints are established. |
| `internal-v2.history-topology-context` | The packet columns plus the maintainer-confirmed comment field form the complete column inventories of HormoneHist_anon, ProcedureHist_anon, and CancerHist_anon. Packet str is cataloged as string and int64 as int64; comment is assessed as string from its free-text description. Every column is conservatively nullable because pandas dtypes and observed levels do not declare source-schema nullability. | The listed columns, including the free-text comment field, are the complete columns of HormoneHist_anon, ProcedureHist_anon, and CancerHist_anon. Their types are parse types, and comment is a free-text string. Every column is treated as nullable because the source declares no schema nullability. |
| `internal-v2.history-topology-context` | In each reviewed packet, the tested PATIENT (empi_anon) grouping is coarser than EXAM (acc_anon), and empi_anon is constant within acc_anon. HormoneHist and ProcHist history attributes vary within the tested patient and exam groupings, except the blank HormoneHist side field. Neither grouping identifies a history row. | In each history table, the patient grouping (empi_anon) is coarser than the exam grouping (acc_anon), and empi_anon is constant within acc_anon. HormoneHist and ProcHist history attributes vary within the patient and exam groupings, except the blank HormoneHist side field. Neither grouping identifies a history row. |
| `internal-v2.history-topology-context` | Reviewed topology packets and the complete-inventory clarification support history table inventories and existing HormoneHist and ProcHist mappings, while preserving unresolved row identity, conditional code discrepancies, and time semantics. | How the HormoneHist, ProcedureHist, and CancerHist tables represent patient history: their complete columns and mappings, with row identity, conditional code discrepancies, and time semantics unresolved. |
| `internal-v2.hormonehist_anon` | Packet int64 is a physical parse type, not evidence for duration units or zero-as-missing semantics. | The int64 type is a parse type and establishes neither duration units nor zero-as-missing semantics. |
| `internal-v2.image.entrance_dose_mgy` | Other represented dose and exposure attributes retain their standard DICOM element meanings but remain inventoried without dedicated catalogue concepts. | Other dose and exposure columns carry their standard DICOM element meanings but are not mapped to features. |
| `internal-v2.join.exam-hormone-history`, `internal-v2.join.exam-procedure-history`, `internal-v2.join.patient-hormone-history`, `internal-v2.join.patient-procedure-history` | Descriptive route through established identifier namespaces; cross-table match coverage was not tested by these packets. | The tables share identifier namespaces; cross-table match coverage is not established. |
| `internal-v2.magview-coded-values-context` | Unexplained tokens are retained as unresolved rather than guessed or silently normalized. | Unexplained tokens have unresolved meanings; do not guess or silently normalize them. |
| `internal-v2.metadata_all_cohorts_v1c` | Detailed DICOM acquisition, dose, detector, collimator, geometry, windowing, and icon-image attributes retain standard DICOM element semantics but remain inventoried without dedicated catalogue concepts or mappings. | Detailed DICOM acquisition, dose, detector, collimator, geometry, windowing, and icon-image columns carry standard DICOM element semantics but are not mapped to features. |
| `internal-v2.pathology.tnm_stage_comment` | This is not a normalized stage category and its content is not exposed by the catalog. | This is not a normalized stage category. |
| `internal-v2.patient-history-context` | The controlled code meanings remain supported, while packet-observed category discrepancies and composite result semantics are separately unresolved. | The controlled code meanings remain supported, while observed category discrepancies and composite result semantics are unresolved. |
| `internal-v2.patient-history-context` | Complete physical inventories and supported mappings are now cataloged from reviewed packets plus maintainer clarification, but no stable history-entry identifier or semantic deduplication rule is established. | The history tables' columns and supported meanings are completely described, but no stable history-entry identifier or semantic deduplication rule is established. |
| `internal-v2.support.patient-history-physical-binding` | Packet dtypes are parse observations, comment is assessed as text, and all columns are conservatively nullable. | Column types are parse types, comment is free text, and every column is treated as nullable. |
| `internal-v2.support.patient-history-physical-binding` | HormoneHist_anon and ProcedureHist_anon have complete physical inventories from reviewed packet columns plus the confirmed comment field, and bindings for supported history semantics. | HormoneHist_anon and ProcedureHist_anon are represented by their complete columns, including the free-text comment field, with mappings for the supported history semantics. |
| `internal-v2.support.v1c-dicom-attribute-interpretation` | An unmapped parsed DICOM column is not semantically unknowable: it can be mapped using the standard element meaning when a dedicated catalogue concept is needed. | An unmapped DICOM column still carries its standard DICOM element meaning. |
| `internal-v2.support.v1c-dicom-attribute-interpretation` | Inventoried internal V1c columns parsed directly from DICOM retain standard element semantics and received no processing beyond anonymization; many acquisition, dose, detector, collimator, geometry, windowing, icon-image, order-number, and time-of-day attributes remain physically inventoried without dedicated catalogue concepts. | Internal V1c columns parsed directly from DICOM carry standard element semantics and received no processing beyond anonymization; many acquisition, dose, detector, collimator, geometry, windowing, icon-image, order-number, and time-of-day columns are not mapped to features. |
| `open-v2.exam_level_anon` | The composite is redundant because the accession identifier is itself unique; it is retained for a directly checked wide-table relationship. | The composite is redundant because the accession identifier is itself unique; a wide-table relationship joins on it. |
| `open-v2.join.imaging-findings-anon-exam` | The accession relationship is expected, but complete referential coverage was not retained as a portable claim. | The accession relationship is expected, but complete referential coverage is not established. |
| `open-v2.join.side-level-anon-exam` | The expected exam hierarchy was not retained as an enforced referential claim. | The exam hierarchy is expected but not established as an enforced referential constraint. |
| `patient` | The catalog does not establish complete longitudinal care or outside-system follow-up. | Complete longitudinal care and outside-system follow-up are not established. |

## Group 2: no single authoritative choice

| Where | Before | After |
|---|---|---|
| `aggregation.pathology-severity-to-finding` | The catalog does not select a finding-level outcome policy. | No single finding-level outcome policy is authoritative. |
| `aggregation.pathology-severity-to-patient` | The catalog does not define incident, prevalent, earliest, or ever-positive patient outcomes. | No single definition of incident, prevalent, earliest, or ever-positive patient outcomes is authoritative. |
| `breast_side` | The semantic object must not inherit one catalog-wide null-laterality rule. | Null laterality has no single meaning across fields; interpret it by each field's clinical role. |
| `clinical.exam-image` | The catalog does not define a portable physical identifier or file-layout rule for associating images with exams. | No physical identifier or file-layout rule for associating images with exams holds across profiles. |
| `guardrail.attached-pathology-selection` | The catalog identifies the selection mechanism but does not prescribe an inclusion, exclusion, weighting, or causal-analysis policy. | No single inclusion, exclusion, weighting, or causal-analysis policy for this selection mechanism is authoritative. |
| `guardrail.explicit-grain-aggregation` | The catalog does not select a canonical cohort or patient-level outcome. | No single cohort or patient-level outcome is authoritative. |
| `guardrail.longitudinal-boundaries-are-analyst-choices` | These choices determine temporal membership and multiplicity but are not representation failures, and the catalog does not select them. | These choices determine temporal membership and multiplicity; they are not representation failures, and no single choice is authoritative. |
| `guardrail.non-breast-cancer-not-no-malignancy` | Whether this state is eligible for any task-specific comparison group is an analysis decision outside the catalog. | Whether this state belongs in a comparison group depends on the task; no single choice is authoritative. |
| `guardrail.null-pathology-not-negative` | A task-specific study may measure follow-up, but the catalog does not supply that policy. | A study may measure follow-up, but no single follow-up policy is authoritative. |
| `internal-v2.image.burned_in_annotation_flag` | The catalogue does not prescribe an action for YES, NO, or absence. | No single action for YES, NO, or absence is authoritative. |
| `internal-v2.magview-representation-context` | The catalog does not select a universal latest-row policy. | No single latest-row policy is authoritative. |
| `internal-v2.v1c-metadata-context` | The catalogue does not prescribe exclusion, redaction, or another action based on this attribute. | No single exclusion, redaction, or other action based on this attribute is authoritative. |
| `open-v2.multimodal-finding-context` | Interpret null laterality according to the field's clinical role rather than applying a catalog-wide null rule. | Interpret null laterality according to the field's clinical role; no single null rule applies to every field. |
| `open-v2.pathology-procedure-context` | Absence of a supplied field does not prohibit an analyst-defined policy; it means the catalog does not designate one. | Absence of a supplied field does not prohibit an analyst-defined policy; no single policy is authoritative. |
| `open-v2.temporal-availability-context` | Task-specific cohort, follow-up, label, and modality-inclusion choices are analysis policies rather than dataset claims; agents must choose and defend them outside the portable catalog. | Cohort, follow-up, label, and modality-inclusion choices are task-specific; no single choice is authoritative. |
