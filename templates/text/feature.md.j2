{#- Features lead with where each profile represents them, since that is
    what an analyst usually needs next. -#}
{% extends "text/_default.md.j2" %}
{% block links %}
{% set columns = d.backlinks | default([]) | selectattr("name", "equalto", "columns") | list %}
{% if columns %}

Represented by
{{ m.link_groups(columns) }}
{%- endif %}
{{ m.links_and_backlinks(d, skip=["columns"]) }}
{%- endblock %}
